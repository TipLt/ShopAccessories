<Window x:Class="BL5_PRN212_MustPass_Project.Views.OrderEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Order Details" Height="600" Width="800"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Info -->
        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0" Grid.Column="0" Text="Customer:" Margin="0,0,0,5"/>
            <ComboBox Grid.Row="1" Grid.Column="0" 
                      ItemsSource="{Binding Customers}"
                      SelectedValue="{Binding OrderDto.CustomerId}"
                      SelectedValuePath="CustomerId"
                      DisplayMemberPath="Name"
                      Margin="0,0,10,0" Padding="5"/>

            <TextBlock Grid.Row="0" Grid.Column="1" Text="Note:" Margin="0,0,0,5"/>
            <TextBox Grid.Row="1" Grid.Column="1" 
                     Text="{Binding OrderDto.Note, UpdateSourceTrigger=PropertyChanged}" 
                     Padding="5"/>
        </Grid>

        <!-- Add Item Section -->
        <Border Grid.Row="1" BorderBrush="#E0E0E0" BorderThickness="1" 
                Padding="10" Margin="0,0,0,10"
                Background="#F9F9F9">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="120"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <ComboBox Grid.Column="0" 
                          ItemsSource="{Binding Products}"
                          SelectedItem="{Binding SelectedProduct}"
                          DisplayMemberPath="Name"
                          Margin="0,0,10,0" Padding="5"/>

                <TextBox Grid.Column="1" 
                         Text="{Binding NewItemQuantity, UpdateSourceTrigger=PropertyChanged}" 
                         Margin="0,0,10,0" Padding="5"/>

                <Button Grid.Column="2" Content="Add Item" 
                        Command="{Binding AddItemCommand}"
                        Padding="15,5" 
                        Background="#4CAF50" Foreground="White" BorderThickness="0"/>
            </Grid>
        </Border>

        <!-- Items List -->
        <DataGrid Grid.Row="2"
                  ItemsSource="{Binding OrderItems}"
                  SelectedItem="{Binding SelectedItem}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Product" Binding="{Binding ProductName}" Width="*" IsReadOnly="True"/>
                <DataGridTextColumn Header="Code" Binding="{Binding ProductCode}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, StringFormat=C}" Width="100" IsReadOnly="True"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Line Total" Binding="{Binding LineTotal, StringFormat=C}" Width="100" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Remove Button and Total -->
        <Grid Grid.Row="3" Margin="0,0,0,20">
            <Button Content="Remove Item" 
                    Command="{Binding RemoveItemCommand}"
                    HorizontalAlignment="Left"
                    Padding="15,5" 
                    Background="#F44336" Foreground="White" BorderThickness="0"/>

            <StackPanel HorizontalAlignment="Right" Orientation="Horizontal">
                <TextBlock Text="Total Amount:" FontWeight="Bold" FontSize="16" Margin="0,0,10,0"/>
                <TextBlock Text="{Binding TotalAmount, StringFormat=C}" FontWeight="Bold" FontSize="16"/>
            </StackPanel>
        </Grid>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Save" 
                    Click="SaveButton_Click"
                    Padding="20,8" Margin="0,0,10,0" 
                    Background="#2196F3" Foreground="White" BorderThickness="0"/>
            <Button Content="Cancel" 
                    IsCancel="True"
                    Padding="20,8"/>
        </StackPanel>
    </Grid>
</Window>
