<UserControl x:Class="BL5_PRN212_MustPass_Project.Views.MyProfileView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="My Profile" FontSize="18" FontWeight="Bold" 
                       VerticalAlignment="Center" Margin="0,0,20,0"/>
            <Button Content="Refresh" Command="{Binding LoadProfileCommand}" 
                    Padding="10,5"/>
        </StackPanel>

        <!-- Profile Content -->
        <Border Grid.Row="1"
                BorderBrush="#E0E0E0" BorderThickness="1" 
                Padding="20" MaxWidth="500" 
                HorizontalAlignment="Left"
                VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Customer Information" FontSize="16" FontWeight="Bold" Margin="0,0,0,20"/>

                <TextBlock Text="Name:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding Customer.Name}" FontSize="14" Margin="0,0,0,15"/>

                <TextBlock Text="Phone:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding Customer.Phone}" FontSize="14" Margin="0,0,0,15"/>

                <TextBlock Text="Email:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBlock Text="{Binding Customer.Email}" FontSize="14" Margin="0,0,0,15"/>

                <TextBlock Text="Status:" FontWeight="SemiBold" Margin="0,0,0,5"/>
                <TextBlock FontSize="14">
                    <Run Text="{Binding Customer.IsActive, Converter={StaticResource BoolToActiveConverter}}"/>
                </TextBlock>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
