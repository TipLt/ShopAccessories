<Window x:Class="BL5_PRN212_MustPass_Project.Views.LoginWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Login - Shop Accessories" Height="350" Width="400"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Shop Accessories Management System" 
                   FontSize="18" FontWeight="Bold" 
                   HorizontalAlignment="Center"/>

        <TextBlock Grid.Row="2" Text="Username:" FontWeight="SemiBold"/>
        <TextBox Grid.Row="4" x:Name="UsernameTextBox"
                 Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}"
                 FontSize="14" Padding="5"/>

        <TextBlock Grid.Row="6" Text="Password:" FontWeight="SemiBold"/>
        <PasswordBox Grid.Row="8" x:Name="PasswordBox" 
                     FontSize="14" Padding="5"/>

        <Button Grid.Row="10" Content="Login" 
                Command="{Binding LoginCommand}"
                CommandParameter="{Binding ElementName=PasswordBox}"
                FontSize="14" Padding="10" 
                Margin="0,10,0,0"
                VerticalAlignment="Top"
                Height="40"/>

        <TextBlock Grid.Row="10" Text="{Binding ErrorMessage}" 
                   Foreground="Red" 
                   TextWrapping="Wrap"
                   VerticalAlignment="Bottom"
                   HorizontalAlignment="Center"/>
    </Grid>
</Window>
